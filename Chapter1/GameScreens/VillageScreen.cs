using GameEngine.State;
using Microsoft.Xna.Framework;

namespace Chapter1.GameScreens
{
    public class VillageScreen : BaseGameState
    {
        public VillageScreen(Game1 game, GameStateManager manager) : base(game, manager)
        {
        }
    }
}